<div class="m-10 mt-5 mb-2 text-center">
 <a href=<%= movie.imdb_page %> target='_blank'>
  <img class="inline" src=<%= movie.poster_img_url %> alt=<%= movie.title %> />
 </a>

 <h2 class="mt-5 text-3xl text-slate-700  truncate"><%= movie.title %></h2>
</div>

