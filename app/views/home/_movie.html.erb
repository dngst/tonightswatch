<div class="mr-20 mb-10 text-center">
 <a href=<%= movie.imdb_page %> target='_blank'>
  <img src=<%= movie.poster_img_url %> alt=<%= movie.title %> />
 </a>

 <h2 class="mt-10 mb-5 text-3xl text-gray-400"><%= movie.title %></h2>
</div>

